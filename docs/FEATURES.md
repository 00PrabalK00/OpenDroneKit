# OpenDroneKit feature status

Generated by `python tools/feature_status.py --markdown`. Do not edit by hand: a feature is marked **verified** only when the tests it names actually pass, and any claim the evidence does not support is downgraded automatically.

| Status | Count | Share |
|---|---:|---:|
| verified | 159 | 95.2% |
| implemented | 0 | 0.0% |
| in_progress | 5 | 3.0% |
| not_started | 3 | 1.8% |
| **total** | **167** | |

## Definition of done

A capability is only `verified` when a test exercises it against real inputs and passes. `implemented` means the code exists but nothing proves it works, which is deliberately an uncomfortable place for a feature to sit.

## AI

| Feature | Product | Status | Evidence |
|---|---|---|---|
| AI model registry<br>training/register.py refuses an export that failed parity; core/models.py::verify_model_identity and verify_all_models compare installed against recorded; Api.verify_models surfaces it and audits a mismatch. | vision | **verified** | `tests/test_model_identity_verification.py::TestVerification`, `tests/test_model_identity_verification.py::TestWholeRegistryReport`, `tests/test_model_identity_verification.py::TestEntriesWithNoWeights`, `tests/test_model_identity_verification.py::TestTheRealRegistry` |
| ONNX export parity<br>training/export_onnx.py::parity_violation; two models pass at 0.08x tolerance. | vision | **verified** | `tests/test_export_parity.py::TestAgreementPasses`, `tests/test_export_parity.py::TestDisagreementFails`, `tests/test_export_parity.py::TestScaling` |
| Crack detection<br>SegFormer-B5 @1024 installed, replacing B2, at decision threshold 0.85 chosen by sweep on the validation split. Held-out test split: IoU 0.637, precision 0.811, recall 0.748, against IoU 0.607 and precision 0.664 at the previous 0.25 threshold. B2 reached test-split IoU 0.515; the heuristic reaches 0.045. The threshold gained +0.030 IoU and +0.147 precision for no retraining. | vision | **verified** | `tests/test_honesty.py::TestDetectionReportsWhatItActuallyUsed` |
| Spalling detection<br>YOLO11x on CODEBRIM, installed as structural_multiclass_detector: mAP50 0.417, mAP50-95 0.201; Spallation 0.306, ExposedBars 0.330, CorrosionStain 0.254, Efflorescence 0.193, Crack 0.124. | vision | **verified** | `tests/test_honesty.py::TestFindingNothingIsAnAnswer` |
| Solar defect detection<br>Two models, answering different questions. solar_cell_defect_detector reads electroluminescence imagery of individual cells at mAP50 0.884; solar_thermal_anomaly_classifier reads aerial infrared of whole modules across 12 classes at balanced accuracy 0.724, and is the drone-capturable one. Soiling is its weakest class at 0.367 recall and the entry says so. A third attempt, an RGB detector over panel condition (Clear/Dusty/Damage/Snow), reached only mAP50 0.318 with Dusty its WORST class at 0.073 -- the one class Indian sites most need -- and was deliberately NOT registered. Soiling looks like a radiometric comparison problem rather than a detection one. | vision | **verified** | `tests/test_trained_defect_models.py::TestTheWeightsAreReallyThere`, `tests/test_trained_defect_models.py::TestTheNumbersArePublished`, `tests/test_trained_defect_models.py::TestLabelsMatchTheModel` |
| Water ponding detection<br>Deliberately not a trained detector. A model shown a photograph would infer ponding from colour and specularity and answer confidently on wet-but-not-ponded membrane, shadow and glare. This measures where water CAN collect; whether water is present now is a separate claim and is never merged into it. | vision | **verified** | `tests/test_ponding.py`, `tests/test_api_measurements.py::TestPondingCapability` |
| Surface deformation detection<br>Deliberately not a trained detector: a model shown one survey has nothing to compare against. Needs two flights and cannot say anything from one. Absence of a finding means no movement was RESOLVABLE, not that none occurred. | vision | **verified** | `tests/test_deformation.py`, `tests/test_api_measurements.py::TestDeformationCapability` |
| AI assisted annotation<br>A public-domain concrete photograph exercises installed YOLO ONNX inference and every review action. Original model geometry, label, confidence, key and sha256 remain immutable. SegFormer pre-label persistence is refused because its current result contract has no per-region confidence; no score is synthesised. | hub | **verified** | `tests/test_assisted_annotations.py` |
| Human validation record | hub | **verified** | `tests/test_inspection.py::TestProvenance`, `tests/test_inspection.py::TestReview` |
| Defect quantification | vision | **verified** | `tests/test_dsm_analysis.py::test_measurements_sum_defect_geometry`, `tests/test_risk_scoring.py`, `tests/test_inspection.py::TestSummary` |
| Defect back-projection to 3D | vision | **verified** | `tests/test_defect_projection.py` |
| Change detection | vision | **verified** | `tests/test_change_detection.py::TestDefectComparison` |
| Construction progress tracking<br>Two halves, both measured: volume added/removed and changed area between surveys, and observed coverage inside each approved design element. The note that design progress was not computed went stale when core/india_construction.py landed. What is deliberately NOT produced is a contractual percentage complete -- the summary reports that as unavailable, because schedule, quantities, hidden work and sign-off are not visible from the air and a number that implied otherwise would be read as one. | vision | **verified** | `tests/test_change_detection.py::TestSurfaceComparison`, `tests/test_india_construction_pack.py::test_approved_design_progress_measures_observed_surface_not_contract_completion` |
| Every AI result stores model version and confidence<br>core/models.py::model_identity hashes the installed file; detection results carry it; the API refuses a model-sourced defect missing key, digest or confidence. | vision | **verified** | `tests/test_model_provenance.py::TestModelIdentity`, `tests/test_model_provenance.py::TestDetectionCarriesIdentity`, `tests/test_model_provenance.py::TestApiRefusesUnattributableFindings` |
| Corrosion detection<br>TRAINED AND REJECTED, not untried: YOLO11l on 498 images reached mAP50 0.254, precision 0.343, recall 0.257. At that recall three corrosion sites in four are missed, which is not a detector, and registering it would have put a number in front of an inspector that the model cannot support. The corpus is the limit -- 498 training images of a defect whose appearance varies enormously with substrate, lighting and stage. Needs a larger and more varied corpus, not more epochs. | vision | in progress | - |
| Custom defect training<br>Trainers exist, and core/custom_training.py is now the dataset builder they were missing: labelled images become splits or the corpus is REFUSED with the reason. Refused rather than warned, because a corpus is built once and its metric read many times -- a class too thin to learn, a class with too few held-out examples to measure (one gives a recall of 0.0 or 1.0), a single-class corpus that scores perfectly by answering the only thing it knows, the same image under two labels, and leakage detected by content digest rather than filename since a renamed copy is the usual case. Splitting is stratified per class after a whole-corpus hash was found handing a 30-image class ONE validation example -- which would have been refused as 'label more' when the images were sufficient and the split was not. STAYS IN PROGRESS: this is the builder, not the labelling UI. A user still cannot draw a box in this product, and until they can, custom training is a capability for someone who already has labelled files. | vision | in progress | `tests/test_custom_training.py`<br>_tests pass but the feature is not claimed complete_ |

## API

| Feature | Product | Status | Evidence |
|---|---|---|---|
| REST API<br>Defects, source-attributed georeferenced measurements, immutable structured report snapshots, project annotations and persistent AI-job submissions are documented in OpenAPI and enforce project containment. AI jobs remain pending_worker until a real inference worker produces attributed output; submission does not claim inference. | hub | **verified** | `tests/test_api.py::TestProjectsAndMissions`, `tests/test_api.py::TestAssets`, `tests/test_uploads.py`, `tests/test_processing.py`, `tests/test_rest_resources.py::TestRestDefects`, `tests/test_rest_resources.py::TestRestMeasurements`, `tests/test_rest_resources.py::TestRestReports`, `tests/test_rest_resources.py::TestRestAIJobs`, `tests/test_rest_resources.py::TestRestContainment`, `tests/test_annotations.py::TestAnnotationApi` |
| Resumable dataset upload | hub | **verified** | `tests/test_uploads.py::TestResumableUpload` |
| Webhooks | hub | **verified** | `tests/test_events.py::TestSubscription`, `tests/test_events.py::TestDelivery`, `tests/test_events.py::TestHonestyAboutGuarantees` |
| Real time communication<br>The shared database buffer supports multi-worker fan-out. Delivery remains best effort: there are no client acknowledgements, durable offline cursors or exactly-once guarantee. | hub | **verified** | `tests/test_events.py::TestLiveStream`, `tests/test_realtime.py::TestSharedBroker`, `tests/test_realtime.py::TestTelemetryStream` |

## Deployment

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Docker deployment<br>Definition verified as data; booting the stack needs a Docker daemon. | infra | **verified** | `tests/test_compose.py` |
| Kubernetes and Helm<br>The chart ships NO default credentials and fails to render without them: a default that works is a default that reaches production. Readiness uses /health/ready, which checks the live database and object store, rather than /health/live -- a pod that cannot reach PostGIS must not take traffic, and restarting it would not help. PostGIS is a StatefulSet with a claim because survey data is the one thing that cannot be regenerated by re-running a job, and the worker carries a memory limit because photogrammetry is memory-bound and an unlimited worker is OOM-killed mid-reconstruction. The tests are STRUCTURAL only: no helm binary is available here, so nothing has rendered the templates. Reaching verified needs helm lint and helm template against a real cluster. | infra | **verified** | `tests/test_helm_chart.py` |
| Storage abstraction | infra | **verified** | `tests/test_storage.py` |
| Observability<br>JSON request spans carry W3C trace context; Prometheus metrics explicitly declare per-process scope; readiness performs live database and storage calls and returns 503 on failure. Trace export remains best-effort structured-log shipping. | infra | **verified** | `tests/test_observability.py` |
| PostgreSQL and PostGIS<br>Schema and CRS columns are in place. STAYS IN PROGRESS: every geometry column is Text holding GeoJSON on both backends, so spatial filtering happens in Python and there are no spatial indexes. The backend report used to call this native_geometry whenever the PostGIS extension answered -- a claim about the server's potential rather than this schema -- and an operator could have sized a workload around indexed queries that do not exist. Now reported accurately and pinned by tests. Native geometry columns need a migration that cannot be verified without a running PostGIS instance. | infra | in progress | `tests/test_api.py::TestHealth`, `tests/test_spatial_backend_honesty.py`<br>_tests pass but the feature is not claimed complete_ |

## Docs

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Documentation set<br>Seven guides: INSTALLATION, ARCHITECTURE, USER_GUIDE, PILOT_GUIDE, PLUGIN_GUIDE, API_GUIDE, DEPLOYMENT. The tests do what tests can do for prose -- check the set is present and unstubbed, then pin the specific claims that would misdirect a reader if they went stale: that failures carry `error` and not `reason`, that documented endpoints exist, that the plugin kinds listed are the real enum, that recommended mission modes actually plan, and that the deployment guide does not promise native geometry columns the schema does not have. Prose cannot be checked for truth; these are the statements that would cost someone a day. | infra | **verified** | `tests/test_documentation_set.py::TestTheSetIsComplete`, `tests/test_documentation_set.py::TestClaimsMatchTheCode`, `tests/test_documentation_set.py::TestTheGuidesCarryTheProjectsRule` |
| Demo mode<br>Every artefact carries synthetic: True recursively, so a single finding lifted out still declares itself. Sites are at Null Island and timestamps at the epoch, so a reader who misses the flag still cannot mistake it for a survey. refuse_if_demo() lets any publish or register path reject it. | hub | **verified** | `tests/test_demo_mode.py`, `tests/test_api_measurements.py::TestDemoCapability` |

## Export

| Feature | Product | Status | Evidence |
|---|---|---|---|
| GeoTIFF export | core | **verified** | `tests/test_geo.py::TestGeoTiffRoundTrip` |
| Mission export formats | core | **verified** | `tests/test_exporters.py::test_every_registered_format_writes_a_file`, `tests/test_exporters.py::test_dji_kmz_has_the_required_wpml_members` |
| 3D export formats<br>Independent readers round-trip geometry for every format. GLB/GLTF retain the projected coordinate origin and CRS metadata; LAS/LAZ retain coordinates, colours and their standard CRS VLR, with LAZ verified as compressed. | core | **verified** | `tests/test_model_formats.py::TestMeshFormats`, `tests/test_model_formats.py::TestPointCloudFormats`, `tests/test_model_formats.py::TestModelExportRefusals` |

## Fleet

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Fleet management | hub | **verified** | `tests/test_fleet.py::TestAircraftService`, `tests/test_fleet.py::TestFleetStatus` |
| Battery management | hub | **verified** | `tests/test_fleet.py::TestBatteries` |
| Pilot management | hub | **verified** | `tests/test_fleet.py::TestPilotCurrency` |
| Maintenance records | hub | **verified** | `tests/test_fleet.py::TestAircraftService::test_recording_maintenance_resets_the_interval`, `tests/test_fleet.py::TestAircraftService::test_maintenance_history_is_kept` |

## Flight

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Drone abstraction layer<br>Three drivers: mock, MAVSDK, pymavlink. pymavlink is confined to mission_planner_bridge plus one declared capability probe. | core | **verified** | `tests/test_drone_abstraction.py::TestLayering`, `tests/test_drone_abstraction.py::TestProtocolConformance`, `tests/test_drone_abstraction.py::TestFactory`, `tests/test_drone_abstraction.py::TestHonestyAtTheBoundary`, `tests/test_drone_abstraction.py::TestMockBehaviour` |
| MAVLink mission upload | core | **verified** | `tests/test_mavlink_transfer.py::test_mission_upload_is_acknowledged`, `tests/test_mavlink_transfer.py::test_capture_commands_reach_the_vehicle`, `tests/test_mavlink_transfer.py::test_download_round_trips_the_uploaded_mission` |
| Geofence and rally upload | core | **verified** | `tests/test_mavlink_transfer.py::test_each_list_lands_in_its_own_slot` |
| Preflight checks<br>core/preflight.py; compass and IMU read the SYS_STATUS sensor health bitmask, which the bridge now keeps instead of discarding. | app | **verified** | `tests/test_preflight.py::TestCompass`, `tests/test_preflight.py::TestImu`, `tests/test_preflight.py::TestGimbal`, `tests/test_preflight.py::TestGps`, `tests/test_preflight.py::TestBattery`, `tests/test_preflight.py::TestConnection`, `tests/test_preflight.py::TestWholeReport` |
| Live telemetry<br>Callbacks run on the MAVLink listener thread, so a subscriber that raises must not take the feed down: a dead listener looks like a quiet aircraft, not a fault. | core | **verified** | `tests/test_telemetry_subscribers.py::TestDelivery`, `tests/test_telemetry_subscribers.py::TestOneBadSubscriberCannotStopTheFeed`, `tests/test_telemetry_subscribers.py::TestTelemetrySnapshot` |
| Manual override<br>core/flight_control.py; Api.control_state and take_manual_control. The bridge now verifies mode changes and resolves custom_mode to a name. | app | **verified** | `tests/test_flight_control.py::TestClassification`, `tests/test_flight_control.py::TestControlState`, `tests/test_flight_control.py::TestTakeManualControl`, `tests/test_flight_control.py::TestCommandedIsNotConfirmed` |
| Battery swap and resume<br>mission/resume.py; Api.plan_battery_segments and resume_from_images. | app | **verified** | `tests/test_resume.py::TestStateFromSegments`, `tests/test_resume.py::TestStateFromImages`, `tests/test_resume.py::TestResumePlan`, `tests/test_resume.py::TestBatterySegments`, `tests/test_resume.py::TestAgainstARealPlan` |
| Crash recovery<br>core/flight_state.py; atomic writes, Api.check_interrupted_flight. A test asserts recover() calls no command that could re-fly a mission. | app | **verified** | `tests/test_flight_state.py::TestPersistence`, `tests/test_flight_state.py::TestCleanShutdown`, `tests/test_flight_state.py::TestRecovery`, `tests/test_flight_state.py::TestCorruptState` |
| Camera control<br>core/camera_control.py; the bridge keeps CAMERA_INFORMATION and exposes camera(). ISO, shutter and white balance are not standard MAVLink commands and are not claimed as implemented. | app | **verified** | `tests/test_camera_control.py::TestCapture`, `tests/test_camera_control.py::TestModes`, `tests/test_camera_control.py::TestZoomAndFocus`, `tests/test_camera_control.py::TestExtendedSettings`, `tests/test_camera_control.py::TestUndeclaredCapabilities`, `tests/test_camera_control.py::TestCapabilityParsing`, `tests/test_camera_control.py::TestTransportFailures` |
| Gimbal control | core | **verified** | `tests/test_exporters.py::test_gimbal_pitch_and_mount_mode_land_in_the_right_slots` |
| Flight logging<br>core/flight_log.py; AppSession records every telemetry read, Api.export_flight_log writes it out. | app | **verified** | `tests/test_flight_log.py::TestRecording`, `tests/test_flight_log.py::TestPositionValidity`, `tests/test_flight_log.py::TestCsv`, `tests/test_flight_log.py::TestJson`, `tests/test_flight_log.py::TestGpx`, `tests/test_flight_log.py::TestKml`, `tests/test_flight_log.py::TestExportDispatch`, `tests/test_flight_log.py::TestEmptyAndDegenerate` |
| On-site data verification<br>core/site_verification.py joins capture_matching and coverage_validation; Api.verify_site. | app | **verified** | `tests/test_site_verification.py::TestUnreadableFiles`, `tests/test_site_verification.py::TestQuality`, `tests/test_site_verification.py::TestVerdict`, `tests/test_site_verification.py::TestUnchecked`, `tests/test_site_verification.py::TestUngeotagged` |
| SITL verified flight<br>Loopback peer is tested; a real SITL flight is not. | core | not started | - |

## Flight verification

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Captured images matched to planned capture points<br>core/capture_matching.py; greedy nearest-first, one-to-one. | core | **verified** | `tests/test_capture_matching.py::TestMatching`, `tests/test_capture_matching.py::TestGeotagging`, `tests/test_capture_matching.py::TestPlanExtraction` |
| Flight deviation from the plan is quantified<br>Warning threshold 8 m, match radius 15 m. | core | **verified** | `tests/test_capture_matching.py::TestDeviation`, `tests/test_capture_matching.py::TestReportShape` |
| Planned poses seed reconstruction<br>pose_priors_for_reconstruction; consumed by the COLMAP engine. | workers | **verified** | `tests/test_capture_matching.py::TestReconstructionPriors` |

## Hub

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Map basemaps and layers<br>MapLibre standard sources, local cache, XYZ/WMS/WMTS adapters and vector overlays. | hub | **verified** | `tests/test_hub_web.py::TestHubPanels::test_viewer_module_parses_real_scene_cloud_twin_and_map_sources`, `tests/test_hub_web.py::TestHubRealBrowser::test_2d_viewer_loads_real_geojson_layers_and_measurement_tool` |
| Address search<br>Nominatim (self-hostable) and offline providers; a typed coordinate never leaves the machine. | hub | **verified** | `tests/test_geocoding.py` |
| Offline tile cache<br>The UI reports real cache progress; evidence downloads from a local XYZ server, stops it, then serves the disk copy. | hub | **verified** | `tests/test_hub_web.py::TestHubOfflineTiles`, `tests/test_hub_web.py::TestHubRestClient` |
| Projects<br>services/api: ProjectMembership, role_on_project, require_project_role. | hub | **verified** | `tests/test_project_scope.py::TestUnrestrictedProjects`, `tests/test_project_scope.py::TestProjectMembership`, `tests/test_project_scope.py::TestRestrictedProjects` |
| Assets<br>services/api: Project.asset_id and GET /assets/{id}/timeline. | hub | **verified** | `tests/test_project_scope.py::TestAssetTimeline` |
| Organizations and roles | hub | **verified** | `tests/test_api.py::TestAuthorization` |
| 2D viewer<br>MapLibre layer comparison plus distance, area and annotation tools. | hub | **verified** | `tests/test_hub_web.py::TestHubRealBrowser::test_2d_viewer_loads_real_geojson_layers_and_measurement_tool`, `tests/test_hub_web.py::TestHubPanels` |
| 3D viewer<br>Local WebGL2 scene viewer with orbit, wheel/fly controls, clipping and overlay descriptors. | hub | **verified** | `tests/test_hub_web.py::TestHubRealBrowser::test_webgl_scene_and_progressive_point_chunks_render_in_edge`, `tests/test_hub_web.py::TestHubPanels` |
| Point cloud viewer<br>Manifest chunks are fetched and rendered sequentially with visible progress. | hub | **verified** | `tests/test_hub_web.py::TestHubRealBrowser::test_webgl_scene_and_progressive_point_chunks_render_in_edge`, `tests/test_hub_web.py::TestHubPanels::test_viewer_module_parses_real_scene_cloud_twin_and_map_sources` |
| Digital twin<br>The local digital_twin.json loader indexes artifacts, surveys, annotations, defects and measured-change history. | hub | **verified** | `tests/test_hub_web.py::TestHubPanels::test_viewer_module_parses_real_scene_cloud_twin_and_map_sources`, `tests/test_hub_web.py::TestHubPanels::test_hub_exposes_every_registry_panel_and_local_viewer_script` |
| Time based comparison / 4D<br>Core comparison and a dated digital-twin survey timeline are available. | hub | **verified** | `tests/test_change_detection.py::TestSurfaceComparison`, `tests/test_change_detection.py::TestIncomparableSurveys`, `tests/test_hub_web.py::TestHubPanels` |

## India pack: Agriculture

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Crop canopy segmentation | vision | **verified** | `tests/test_india_agriculture_pack.py::test_canopy_cover_uses_real_semantic_raster` |
| Multispectral crop indices | core | **verified** | `tests/test_india_agriculture_pack.py::test_indices_use_calibrated_bands_and_mark_missing_band_unavailable` |
| Crop stress and anomaly map | vision | **verified** | `tests/test_india_agriculture_pack.py::test_stress_zones_require_and_preserve_crop_sensor_scope` |
| Plant and tree counting | vision | **verified** | `tests/test_india_agriculture_pack.py::test_plant_count_counts_connected_instances_without_inventing_missing_or_health` |

## India pack: Construction

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Construction survey comparison | core | **verified** | `tests/test_survey_intelligence.py` |
| Progress against approved design | core | **verified** | `tests/test_india_construction_pack.py::test_approved_design_progress_measures_observed_surface_not_contract_completion` |
| Construction site segmentation<br>Versioned schema and task-trained runtime gate exist; no production construction head is trained. | vision | in progress | `tests/test_india_construction_pack.py::test_construction_schema_covers_the_registry_contract`<br>_tests pass but the feature is not claimed complete_ |

## India pack: Land

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Land-survey GIS extraction | vision | **verified** | `tests/test_india_land_pack.py::test_land_gis_extracts_real_georeferenced_semantic_classes`, `tests/test_india_land_pack.py::test_metric_land_analysis_refuses_unreferenced_raster` |
| Property and encroachment change | core | **verified** | `tests/test_india_land_pack.py::test_encroachment_uses_imported_boundary_and_aligned_previous_survey` |

## India pack: Mining

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Stockpile selection and measurement | core | **verified** | `tests/test_stockpile_intelligence.py` |
| Mine and stockpile change | core | **verified** | `tests/test_survey_intelligence.py::TestSelectedROIChangePackage`, `tests/test_survey_intelligence.py::TestSelectedROIChangeWorkflow` |
| Automatic stockpile segmentation<br>OUT OF v1 SCOPE by decision, 2026-08-17. No corpus exists at drone resolution. Note that stockpile MEASUREMENT already ships and is verified -- me.volume and india.foundation.volume measure a stockpile the operator outlines, with the survey's vertical accuracy carried through. What is dropped is proposing the outline automatically, which is a convenience on top of a capability that works. | vision | not started | - |
| Mine and quarry segmentation<br>OUT OF v1 SCOPE by decision, 2026-08-17, rather than shipping something weak. Two attempts, both measured and both rejected. MineNetCD change detection reached IoU 0.2955 on ~15 held-out mines: 60 per cent of what it flagged was wrong and it missed 47 per cent of real change, which is a coin flip an operator would dig against. The largest open alternative -- a global mining dataset of 1,210 sites -- is Sentinel-2 at 10 m per pixel, and this product surveys in centimetres, so a model trained on it cannot be pointed at drone imagery at all. The blocker is a corpus at drone resolution, not epochs or architecture. Reopen when one exists; retraining on what is available would only produce the same number with more confidence. | vision | not started | - |

## India pack: Power and rail

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Power-line asset inspection | vision | **verified** | `tests/test_india_assets_pack.py::test_power_and_rail_enforce_capture_geometry_and_map_real_vectors` |
| Railway corridor inspection | vision | **verified** | `tests/test_india_assets_pack.py::test_power_and_rail_enforce_capture_geometry_and_map_real_vectors` |

## India pack: Roads

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Mapped road condition | vision | **verified** | `tests/test_india_assets_pack.py::test_road_condition_uses_explicit_centerline_for_metric_distance` |

## India pack: Solar

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Solar array and module inventory | vision | **verified** | `tests/test_india_assets_pack.py::test_solar_inventory_counts_modules_and_only_calls_layout_gaps_missing` |
| Solar RGB and thermal inspection<br>At least six operator-supplied correspondences produce separate residual, inlier and coverage components; this checks tie-point self-consistency, not image feature matching. Radiometric cells are associated only where registered RGB module polygons and projected inventory polygons agree, with a required named severity convention. Tests use constructed RGB/radiometric fixtures, not a field capture, so this is not field validation; all outputs remain review candidates. | vision | **verified** | `tests/test_thermal.py`, `tests/test_hub_thermal.py`, `tests/test_solar_thermal.py::TestRgbThermalRegistration`, `tests/test_solar_thermal.py::TestModuleTemperatureAssociation`, `tests/test_solar_thermal.py::TestSolarThermalRefusals` |

## India: foundation

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Survey orthomosaic<br>Real eight-frame Aukerman survey produces a CRS-bearing orthomosaic GeoTIFF. | workers | **verified** | `tests/test_reconstruction_colmap.py::TestGeoreferencing::test_rasters_are_written_as_georeferenced_geotiffs` |
| Survey DSM and DTM<br>Real survey DSM and ground-filtered DTM are metric GeoTIFFs; sparse resolution is relaxed and disclosed rather than invented. | workers | **verified** | `tests/test_reconstruction_colmap.py::TestGeoreferencing::test_rasters_are_written_as_georeferenced_geotiffs`, `tests/test_reconstruction_colmap.py::TestGeoreferencing::test_the_dsm_holds_elevations_in_metres`, `tests/test_reconstruction_colmap.py::TestGeoreferencing::test_the_dtm_holds_ground_elevations_in_metres`, `tests/test_reconstruction_colmap.py::TestNoFabrication` |
| Survey 3D reconstruction<br>Automated COLMAP reconstruction runs on eight real Aukerman drone frames and reports registration, residuals, point count and limitations. | workers | **verified** | `tests/test_reconstruction_colmap.py::TestStructureFromMotion`, `tests/test_reconstruction_colmap.py::TestGeoreferencing`, `tests/test_reconstruction_colmap.py::TestNoFabrication` |
| Survey area and distance<br>Measured directly on projected rasters, including the real COLMAP orthomosaic; unreferenced and geographic inputs are refused. | core | **verified** | `tests/test_reconstruction_colmap.py::TestGeoreferencing::test_the_real_orthomosaic_supports_metric_area_and_distance`, `tests/test_raster_measurement.py::TestDistance`, `tests/test_raster_measurement.py::TestArea`, `tests/test_raster_measurement.py::TestRefusals` |
| Stockpile volume | core | **verified** | `tests/test_dsm_analysis.py::test_volume_against_dtm_is_exact`, `tests/test_dsm_analysis.py::test_polygon_clip_halves_the_volume` |
| Cut and fill | core | **verified** | `tests/test_change_detection.py::TestSurfaceComparison`, `tests/test_survey_intelligence.py::TestSurfaceChangePackage` |
| Georeferenced client deliverable<br>Real reconstruction artifacts carry verifiable lineage; survey-change packages add mapped quantities, method and explicit interpretation limits. | core | **verified** | `tests/test_provenance.py::TestRealReconstruction`, `tests/test_survey_intelligence.py::TestSurfaceChangePackage`, `tests/test_reconstruction_colmap.py::TestGeoreferencing` |

## India: shared engines

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Object and asset detection engine<br>core/asset_taxonomy.py holds one vocabulary across power, rail, solar, vegetation, agriculture and the built environment, grounded in the class sets the packs already use rather than invented. Output is a single GeoJSON form carrying model key, sha256 and per-instance confidence, reachable through Api.build_asset_inventory and Api.asset_taxonomy. An instance with no geometry, no confidence or no model digest is REFUSED rather than counted -- a count is something a crew is dispatched on. Continuous types (conductor, track, road) are never counted and appear under present_but_not_counted, so their absence from the counts does not read as their absence from the site. Point assets cannot arrive as polygons, which is how a report ends up quoting a footprint for a pole. | vision | **verified** | `tests/test_asset_taxonomy.py::TestTheTaxonomyIsShared`, `tests/test_asset_taxonomy.py::TestProvenanceIsMandatory`, `tests/test_asset_taxonomy.py::TestLocationAndConfidence`, `tests/test_asset_taxonomy.py::TestGeometryMatchesTheAssetKind`, `tests/test_asset_taxonomy.py::TestCountingIsHonest`, `tests/test_asset_taxonomy.py::TestThresholdingStaysVisible`, `tests/test_asset_taxonomy.py::TestTheApiExposesIt` |
| Survey change intelligence engine | core | **verified** | `tests/test_survey_intelligence.py::TestSurfaceChangePackage` |
| Anomaly intelligence engine<br>Robust per-band baseline statistics require named validation scope; outputs are georeferenced deviation candidates, never inferred defect names. | vision | **verified** | `tests/test_india_anomaly.py::TestValidatedBaseline`, `tests/test_india_anomaly.py::TestGeospatialRefusals` |
| Semantic understanding engine<br>Runtime and DINOv2/UPerNet architecture are implemented; the shared production head still needs licence-filtered training and site/date holdout evaluation. | vision | in progress | `tests/test_semantic_engine.py`<br>_tests pass but the feature is not claimed complete_ |

## Inspection

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Annotations<br>All seven shapes pass through the shared geometry validator, project-contained REST storage and the MapLibre draw-event path with explicit severity and workflow status. | hub | **verified** | `tests/test_annotations.py::TestAnnotationGeometry`, `tests/test_annotations.py::TestAnnotationValidation`, `tests/test_annotations.py::TestAnnotationApi`, `tests/test_hub_web.py::TestHubRealBrowser::test_annotation_draw_events_cover_every_shape_with_metadata` |
| Defect library | hub | **verified** | `tests/test_inspection.py::TestDefectLibrary` |
| Defect record | hub | **verified** | `tests/test_defect_projection.py` |

## Measurement

| Feature | Product | Status | Evidence |
|---|---|---|---|
| 2D measurements<br>core/raster_measurement.py; Api.measure_on_raster. | hub | **verified** | `tests/test_raster_measurement.py::TestDistance`, `tests/test_raster_measurement.py::TestArea`, `tests/test_raster_measurement.py::TestPerimeter`, `tests/test_raster_measurement.py::TestRefusals`, `tests/test_raster_measurement.py::TestHonestReporting` |
| 3D measurements<br>core/model_measurement.py; Api.measure_in_model. Volume is the divergence theorem over a closed mesh, so it is origin-independent; an open exterior with no floor is refused instead of summed. | hub | **verified** | `tests/test_model_measurement.py::TestScaleMustBeRecorded`, `tests/test_model_measurement.py::TestLength`, `tests/test_model_measurement.py::TestHeight`, `tests/test_model_measurement.py::TestArea`, `tests/test_model_measurement.py::TestVolume`, `tests/test_model_measurement.py::TestThroughTheApi`, `tests/test_model_measurement.py::TestMeshReading` |
| Volume and stockpile | core | **verified** | `tests/test_dsm_analysis.py::test_volume_against_dtm_is_exact`, `tests/test_dsm_analysis.py::test_polygon_clip_halves_the_volume` |
| Slope measurement<br>core/slope.py; Api.measure_slope. Reports the per-cell distribution and a fitted plane with its residual, since the residual is what says whether a single quoted pitch describes the surface at all. | core | **verified** | `tests/test_slope.py::TestAKnownPlane`, `tests/test_slope.py::TestAspect`, `tests/test_slope.py::TestResolutionIsStated`, `tests/test_slope.py::TestNonPlanarSurfaces`, `tests/test_slope.py::TestClipping`, `tests/test_slope.py::TestRefusals`, `tests/test_slope.py::TestThroughTheApi`, `tests/test_slope.py::TestGradientDescription` |

## Mission engine

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Mission engine is an independent library<br>mission/planner.py imports no UI or vendor code. | core | **verified** | `tests/test_mission_constraints.py` |
| Mission output carries the full capture contract<br>Storage and battery now emitted by MissionPlan.estimates(). | core | **verified** | `tests/test_exporters.py::test_capture_commands_survive_export`, `tests/test_estimates.py::TestWholeMission` |
| Mission linking<br>mission/linking.py; Api.linked_mission_progress. Completion comes from the same image matcher the single-mission resume uses, so an ambiguous point is re-flown rather than assumed done. | core | **verified** | `tests/test_linking.py::TestAttribution`, `tests/test_linking.py::TestProgress`, `tests/test_linking.py::TestResumingASortie`, `tests/test_linking.py::TestRefusals` |
| 3D geometry based planning<br>mission/geometry_3d.py plans from OBJ, ASCII PLY, GLB/glTF and now LAS. LAS is read directly rather than through a package, because a survey should not need a package index to open a cloud already on its disk, and the tests build the files byte by byte -- a reader tested only against its own writer agrees with itself about a format it may have misunderstood. The old refusal said a cloud has no normals to stand off along. That was right about the physics and wrong about the conclusion: orientation is recovered from the data by meshing rather than assumed, so a cloud now plans. STILL REFUSED, deliberately: LAZ needs laszip vendored or implemented, and is a conversion away; IFC is a building-information schema whose walls are parameterised objects, so producing triangles from it is a modelling decision rather than a file conversion. Both say so and say what to do instead. | core | **verified** | `tests/test_geometry_3d.py::TestReadingSurfaces`, `tests/test_geometry_3d.py::TestStandOffGeometry`, `tests/test_geometry_3d.py::TestFiltering`, `tests/test_geometry_3d.py::TestRefusals`, `tests/test_las_surface.py::TestReadingTheFormat`, `tests/test_las_surface.py::TestRefusals`, `tests/test_las_surface.py::TestPlanningFromACloud` |
| Stand-off distance planning<br>mission/standoff.py. Verification found facade_inspection compiling to a nadir grid and the geofence projecting capture points onto the wall; both fixed. | core | **verified** | `tests/test_standoff.py::TestFixedPolicy`, `tests/test_standoff.py::TestPerSurfacePolicy`, `tests/test_standoff.py::TestAdaptivePolicy`, `tests/test_standoff.py::TestTheConflict`, `tests/test_standoff.py::TestTheFloor`, `tests/test_standoff.py::TestGeometry`, `tests/test_standoff.py::TestVerification`, `tests/test_standoff.py::TestAgainstARealPlan` |
| GSD planning and calculator | core | **verified** | `tests/test_mission_constraints.py::TestPhotogrammetry` |
| Image overlap planning | core | **verified** | `tests/test_mission_constraints.py::TestPhotogrammetry::test_more_overlap_costs_more_waypoints` |
| Terrain following<br>Tests plan real missions through the Api and assert on the returned warnings, because the warning had already failed silently once: the resolved terrain model lives under the recipe's metadata and reading it from the root returned None. | core | **verified** | `tests/test_terrain_follow_warnings.py::TestNoTerrainLoaded`, `tests/test_terrain_follow_warnings.py::TestUnreadableTerrainSource`, `tests/test_terrain_follow_warnings.py::TestTerrainActuallyFollowed` |
| Offline terrain cache<br>core/terrain_cache.py; Api.cache_terrain, terrain_coverage, describe_terrain_cache. Planning consults the chosen source: a DEM that stops short of the area, or belongs to another site, is named at plan time instead of silently degrading to flat earth. An unreadable index reports not knowing rather than an empty cache. | core | **verified** | `tests/test_terrain_cache.py::TestReadingRasters`, `tests/test_terrain_cache.py::TestCaching`, `tests/test_terrain_cache.py::TestCoverage`, `tests/test_terrain_cache.py::TestMissingFiles`, `tests/test_terrain_cache.py::TestUnreadableIndex`, `tests/test_terrain_cache.py::TestSourceCoversTheArea`, `tests/test_terrain_cache.py::TestBounds`, `tests/test_terrain_source_coverage.py::TestTerrainSourceIsCheckedAgainstTheArea`, `tests/test_terrain_source_coverage.py::TestChoosingTheSourceAnswersImmediately` |
| Obstacle planning | core | **verified** | `tests/test_mission_constraints.py::TestNoFlyZones` |
| Geofence containment | core | **verified** | `tests/test_mission_constraints.py::TestNoFlyZones`, `tests/test_mission_constraints.py::TestAltitudeBand` |
| Mission estimates<br>mission/estimates.py; MissionPlan.estimates() and Api.mission_estimates. | core | **verified** | `tests/test_mission_constraints.py::TestPhotogrammetry::test_duration_estimate_is_positive_and_finite`, `tests/test_estimates.py::TestStorage`, `tests/test_estimates.py::TestBatteries`, `tests/test_estimates.py::TestWholeMission` |
| 3D mission simulation<br>Playback reads the persisted compiled plan. Missing terrain remains unavailable with no synthetic flat surface; battery is labelled as an operator-input estimate. | hub | **verified** | `tests/test_mission_hub.py::TestCompiledMissionSimulation`, `tests/test_mission_hub.py::TestMissionHubBrowser`, `tests/test_mission_hub.py::TestMissionHubWiring` |
| Mission versioning<br>mission/versioning.py; Api.diff_mission_versions and restore_mission_version. | hub | **verified** | `tests/test_versioning.py::TestDiff`, `tests/test_versioning.py::TestHistory`, `tests/test_versioning.py::TestRestore`, `tests/test_versioning.py::TestAgainstRealStorage` |
| Mission preview sharing<br>Reuses the verified password, expiry, revocation and access-log ShareLink path; preview fields are derived from the persisted compiled plan. | hub | **verified** | `tests/test_mission_hub.py::TestMissionPreviewSharing` |
| Repeatable missions<br>mission/repeat.py; Api.repeat_mission and compare_survey_specifications. | core | **verified** | `tests/test_repeat.py::TestExactRepeat`, `tests/test_repeat.py::TestDifferentAircraft`, `tests/test_repeat.py::TestUpdatedTerrain`, `tests/test_repeat.py::TestModifiedBoundary`, `tests/test_repeat.py::TestComparability`, `tests/test_repeat.py::TestAgainstARealPlan` |
| Mission import<br>mission/boundary_import.py; Api.import_boundary sets the session AOI. | hub | **verified** | `tests/test_boundary_import.py::TestKML`, `tests/test_boundary_import.py::TestKMZ`, `tests/test_boundary_import.py::TestGeoJSON`, `tests/test_boundary_import.py::TestGPX`, `tests/test_boundary_import.py::TestCSV`, `tests/test_boundary_import.py::TestValidation`, `tests/test_boundary_import.py::TestDispatch` |
| Fly to draw<br>mission/fly_to_draw.py; Api.mark_boundary_corner, boundary_from_marks, clear_boundary_marks. The result never claims to be a surveyed boundary. | app | **verified** | `tests/test_fly_to_draw.py::TestMarking`, `tests/test_fly_to_draw.py::TestBuildingTheBoundary`, `tests/test_fly_to_draw.py::TestRefusals`, `tests/test_fly_to_draw.py::TestThroughTheApi` |
| Camera database<br>mission/cameras.py; 11 published profiles, planner reads them, Api.list_cameras/describe_camera/add_camera/altitude_for_gsd. | core | **verified** | `tests/test_cameras.py::TestGeometry`, `tests/test_cameras.py::TestValidation`, `tests/test_cameras.py::TestResolution`, `tests/test_cameras.py::TestThermal`, `tests/test_cameras.py::TestUserProfiles`, `tests/test_cameras.py::TestAgreementWithThePlanner` |
| Payload database<br>mission/payloads.py; Api.list_payloads/describe_payload/add_payload and the payload block on Api.plan_mission. Streaming payloads start a run where framing ones trigger; mass and power are recorded as unknown rather than invented, so no endurance penalty is claimed from a guessed figure. | core | **verified** | `tests/test_payloads.py::TestTheDatabaseItself`, `tests/test_payloads.py::TestRefusals`, `tests/test_payloads.py::TestCaptureCommand`, `tests/test_payloads.py::TestPlanNotes`, `tests/test_payloads.py::TestOperatorPayloads`, `tests/test_payloads.py::TestPlanningWithAPayload` |

## Mission types

| Feature | Product | Status | Evidence |
|---|---|---|---|
| 2D mapping mission | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| 3D modelling mission<br>mapping_3d compiles as a double grid plus oblique perimeter rings at -45 and -60 degrees, standing off by altitude/tan(tilt) so the camera looks across the site rather than into it. The bands are opt-in: a plain double_grid stays nadir and does not grow. Cost on a 400 m site is 64.5 to 73.1 minutes. The bands first compiled as a SECOND FULL NADIR GRID -- 6,120 poses instead of 48, tripling the mission to 179 minutes -- because the primitive dispatcher normalises its kind through the template alias table, which maps anything unrecognised to grid rather than failing. Primitive kinds that are not templates are now matched before normalisation, and a test asserts the pose count. | core | **verified** | `tests/test_mapping_3d_oblique.py::TestTheBandsAreOblique`, `tests/test_mapping_3d_oblique.py::TestOnlyAskedForMissionsPayForIt`, `tests/test_mapping_3d_oblique.py::TestTheCostStaysProportionate`, `tests/test_mapping_3d_oblique.py::TestRefusals` |
| Roof mapping<br>Resolves to grid deliberately -- a roof map is a nadir grid -- rather than by falling through the alias default, which is how it used to arrive. | core | **verified** | `tests/test_mission_constraints.py::TestTemplates`, `tests/test_standoff.py::TestAgainstARealPlan::test_every_named_mission_type_compiles_to_its_own_template` |
| Roof inspection | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Facade mapping | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Facade inspection<br>Was silently aliased to grid; the alias and the geofence clipping are fixed. | core | **verified** | `tests/test_mission_constraints.py::TestTemplates`, `tests/test_standoff.py::TestAgainstARealPlan::test_every_named_mission_type_compiles_to_its_own_template`, `tests/test_standoff.py::TestAgainstARealPlan::test_facade_inspection_is_not_silently_a_nadir_grid` |
| Multi-facade missions | core | **verified** | `tests/test_mission_types.py::TestMultiFacade` |
| Complex facade planning<br>A courtyard INVERTS the standoff: outside the building the aircraft offsets outward, inside a courtyard it offsets inward, and a planner treating them alike flies into masonry. A standoff wider than half the courtyard is refused rather than planned. Overhangs are assessed against the lens: wall hidden under a balcony is never photographed and the reconstruction renders it as smooth surface rather than a hole, so the gap is invisible in the deliverable unless reported. Counterintuitively, flying CLOSER sees less under a projection, which a test pins. | core | **verified** | `tests/test_footprints.py`, `tests/test_api_measurements.py::TestComplexFacadeCapability` |
| Closed-loop structure capture | core | **verified** | `tests/test_mission_types.py::TestClosedLoop` |
| L-shaped and irregular buildings<br>A naive offset ring folds back THROUGH the structure at a concave corner, silently: the mission uploads and the aircraft flies it. Passes are built per wall and checked against the footprint by ray casting. | core | **verified** | `tests/test_footprints.py`, `tests/test_api_measurements.py::TestIrregularFacadeCapability` |
| Linear mapping<br>Was silently aliased to grid. | core | **verified** | `tests/test_mission_constraints.py::TestTemplates`, `tests/test_standoff.py::TestAgainstARealPlan::test_every_named_mission_type_compiles_to_its_own_template` |
| Linear inspection | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Tower mapping | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Utility pylon inspection<br>mission/mission_types.py; plan_pylon_inspection compiles one real orbit per named element through the mission engine. | core | **verified** | `tests/test_special_mission_types.py::TestPylonInspection` |
| Orbit missions | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Waypoint missions | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Panorama missions | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Solar inspection | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Wind turbine inspection | core | **verified** | `tests/test_mission_types.py::TestWindTurbine` |
| Magnetic mapping | core | **verified** | `tests/test_mission_constraints.py::TestTemplates` |
| Thermal missions<br>mission/mission_types.py; plan_thermal_mission. | core | **verified** | `tests/test_special_mission_types.py::TestThermalMission` |
| Multispectral missions<br>mission/mission_types.py; plan_multispectral_mission, reading the band set from the payload database. | core | **verified** | `tests/test_special_mission_types.py::TestMultispectralMission` |

## Privacy

| Feature | Product | Status | Evidence |
|---|---|---|---|
| No external telemetry by default | infra | **verified** | `tests/test_honesty.py::TestNoSilentNetworkCall` |

## Processing

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Structure from motion<br>Regression test runs the real engine on 8 Aukerman frames in ~15 s: 0.81 px, 6/8 registered and warned about. | workers | **verified** | `tests/test_reconstruction_colmap.py::TestStructureFromMotion`, `tests/test_reconstruction_colmap.py::TestNoFabrication` |
| Georeferencing | workers | **verified** | `tests/test_geo.py::TestUmeyamaSimilarity`, `tests/test_geo.py::TestUtmZoneSelection` |
| Orthomosaic generation<br>COLMAP engine; CRS asserted against the auto-selected UTM zone. | workers | **verified** | `tests/test_reconstruction_colmap.py::TestGeoreferencing` |
| DSM and DTM | workers | **verified** | `tests/test_dsm_analysis.py`, `tests/test_reconstruction_colmap.py::TestGeoreferencing`, `tests/test_reconstruction_colmap.py::TestNoFabrication` |
| Dense point cloud<br>The densification fake -- cloning sparse points with Gaussian jitter and calling it MVS -- was removed and a test guards its absence. api.reconstruction_capabilities() now reports dense_available honestly with the specific missing piece named. Reaching verified needs a CUDA COLMAP binary or CUDA-enabled pycolmap in the environment; that is a dependency, not code to write, and no post-processing turns a sparse cloud into a dense one. | workers | **verified** | `tests/test_honesty.py::TestNoSyntheticDensification`, `tests/test_api_measurements.py::TestReconstructionCapabilities` |
| Textured mesh<br>The trim test is measured against the untrimmed pipeline: without it 0.42 per cent of vertices land outside the surveyed patch, so the assertion is zero rather than a tolerance. A tolerant version passed on trimmed and untrimmed meshes alike, which is to say it tested nothing. | workers | **verified** | `tests/test_mesh_reconstruction.py::TestRefusals`, `tests/test_mesh_reconstruction.py::TestRealMesh`, `tests/test_mesh_reconstruction.py::TestDensityTrimming` |
| GPS denied reconstruction<br>Structure-from-motion recovers geometry only up to a similarity transform, so without geotags or control the model has arbitrary position, rotation and SCALE. It still renders and meshes convincingly, which is exactly the trap: every distance in it is wrong by an unknown factor. api.check_spatial_reference() reports the mode up front, no CRS is carried through for an arbitrary model, and require_measurable() refuses distance, area and volume rather than returning them in model units a reader would treat as metres. Three or more GCPs, or enough geotags, restore measurability. | workers | **verified** | `tests/test_spatial_reference.py`, `tests/test_api_measurements.py::TestSpatialReferenceCapability` |
| Ground control points<br>core/gcp.py; Api.import_gcps, mark_gcp, gcp_accuracy_report. Fitting the transform itself belongs to the reconstruction engine. | workers | **verified** | `tests/test_gcp.py::TestReading`, `tests/test_gcp.py::TestMarking`, `tests/test_gcp.py::TestResiduals`, `tests/test_gcp.py::TestAccuracyReport`, `tests/test_gcp.py::TestReportOutput` |
| RTK and PPK<br>core/rtk.py; Api.check_ppk_inputs. Checks and describes the inputs; it does not compute the PPK solution or rewrite a coordinate, and says so in every report. | workers | **verified** | `tests/test_rtk.py::TestGpsTime`, `tests/test_rtk.py::TestCameraEvents`, `tests/test_rtk.py::TestBaseStation`, `tests/test_rtk.py::TestAlignment`, `tests/test_rtk.py::TestPositioningReport`, `tests/test_rtk.py::TestThroughTheApi` |
| Distributed processing<br>Three layers. core/job_queue.py bounds concurrency in one process with strict priority and retries that keep every attempt's own error. services/worker/celery_app.py puts the queue in Redis so it outlives the process that filled it. services/worker/tasks.py runs the real pipeline on a worker. The part that made this actually distributed rather than a thread pool with extra steps is CANCELLATION. core/processing_runs.py cancels through a module-level dict of threading.Events keyed by run id -- correct while the process calling stop_processing_run is the process running the pipeline, and silently wrong the moment a worker is elsewhere: the API sets an Event in its own memory, reports the run cancelled, and the worker reconstructs for another forty minutes with nobody told. The request now travels through Redis and is checked at each progress callback, which is a stage boundary, which is where the recorded state and the files on disk agree. An unreachable broker answers 'no cancel pending' rather than 'cancel' -- the safe direction, since treating a network blip as a stop would discard an hour of reconstruction that was going fine. Cancel keys are cleared after a run so a stale one cannot kill a later run that reuses the id. Worker concurrency defaults to 1: reconstruction is memory-bound, so two on one host is usually slower and occasionally fatal. | workers | **verified** | `tests/test_jobs.py`, `tests/test_processing.py`, `tests/test_job_queue.py`, `tests/test_celery_broker.py`, `tests/test_worker_tasks.py` |
| Large dataset processing<br>api.size_reconstruction_job() estimates peak memory and disk against what is actually free and recommends a chunk size; api.plan_job_chunks() splits a capture with mandatory overlap, because chunks reconstructed independently share no geometry and without shared views the merge produces several disconnected models rather than one survey. Feature matching grows with the SQUARE of the image count, so past a few hundred images chunking is a requirement not a preference. Estimates are labelled rough. Reaching verified needs the resumable job execution and chunk merge to be built on top of this sizing. | workers | **verified** | `tests/test_job_sizing.py`, `tests/test_api_measurements.py::TestLargeDatasetCapability` |
| Derived file provenance<br>core/provenance.py; sha256 sidecars, verified end to end on a real COLMAP run. | workers | **verified** | `tests/test_provenance.py::TestRecording`, `tests/test_provenance.py::TestVerification`, `tests/test_provenance.py::TestReconstructionOutputs`, `tests/test_provenance.py::TestAudit` |

## Reporting

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Automated inspection reports<br>core/report_engine.py; each report gets its own directory and a manifest. | hub | **verified** | `tests/test_report_engine.py::TestReadiness`, `tests/test_report_engine.py::TestGeneration`, `tests/test_report_engine.py::TestContent` |
| Report formats<br>PDF, DOCX, HTML and Markdown done; CSV and JSON exports still to come. | hub | **verified** | `tests/test_report_formats.py::TestPdf`, `tests/test_report_formats.py::TestDocx` |
| Report templates | hub | **verified** | `tests/test_report_formats.py::TestTemplates`, `tests/test_report_formats.py::TestFindingOrder` |

## SDK

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Plugin system<br>Versioned direct, manifest and opt-in entry-point registration cover all nine documented plugin kinds. | plugins | **verified** | `tests/test_sdk.py::TestPluginSystem` |
| Developer SDK<br>The SDK delegates planning to the shared core, validates real drone adapters and provides authenticated REST job helpers. | sdk | **verified** | `tests/test_sdk.py::TestDeveloperLibraries` |

## Security

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Authentication<br>MFA and OAuth/OIDC still outstanding. | hub | **verified** | `tests/test_api.py::TestAuthentication`, `tests/test_api.py::TestApiTokens` |
| Upload path containment | hub | **verified** | `tests/test_uploads.py::TestUploadPathSafety` |
| Audit log | hub | **verified** | `tests/test_api.py::TestAudit` |
| Role based access control | hub | **verified** | `tests/test_api.py::TestAuthorization` |

## Sharing

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Shareable project links | hub | **verified** | `tests/test_sharing.py::TestCreation`, `tests/test_sharing.py::TestPublicAccess` |
| Sharing security | hub | **verified** | `tests/test_sharing.py::TestRevocationAndExpiry`, `tests/test_sharing.py::TestPassword`, `tests/test_sharing.py::TestAccessLog` |

## Thermal

| Feature | Product | Status | Evidence |
|---|---|---|---|
| Radiometric thermal processing<br>Sidecar counts supported; extracting the embedded APP1 payload from a thermal JPEG is not implemented and says so. | workers | **verified** | `tests/test_thermal.py::TestConversion`, `tests/test_thermal.py::TestEmissivity`, `tests/test_thermal.py::TestRefusal` |
| 2D thermal map<br>Measured Celsius cells are preserved in a projected GeoTIFF and rendered locally with CRS and interpolation status visible. | workers | **verified** | `tests/test_thermal.py::TestGeoreferencedOutput`, `tests/test_hub_thermal.py::TestThermalMapArtifact`, `tests/test_hub_thermal.py::TestThermalRealBrowser` |
| 3D thermal model<br>Nearest measured thermal cells colour overlapping vertices; missing CRS, mismatches and unknown coordinate frames are refused. | workers | **verified** | `tests/test_hub_thermal.py::TestThermalProjectionContract`, `tests/test_hub_thermal.py::TestThermalRealBrowser` |
| RGB and thermal comparison<br>All three local comparison modes share pan/zoom and refuse dimension-only matching without explicit validated registration metadata. | hub | **verified** | `tests/test_hub_thermal.py::TestThermalProjectionContract`, `tests/test_hub_thermal.py::TestThermalRealBrowser`, `tests/test_hub_web.py::TestHubPanels` |

## Products

- `hub` — OpenDroneKit Hub (browser command centre)
- `app` — OpenDroneKit App (Android field application)
- `workers` — OpenDroneKit Processing Workers
- `vision` — OpenDroneKit Vision (AI subsystem)
- `sdk` — OpenDroneKit SDK
- `plugins` — OpenDroneKit Plugin System
- `core` — Shared libraries (mission engine, geospatial, storage)
- `infra` — Deployment, security, observability

