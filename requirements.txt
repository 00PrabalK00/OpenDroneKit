# Core runtime
numpy>=1.24
opencv-python>=4.8
scipy>=1.11
scikit-image>=0.22
Pillow>=10.0
Jinja2>=3.1
PyYAML>=6.0
requests>=2.31
tqdm>=4.66

# Desktop shell (replaces PyQt6). On Windows this uses the built-in Edge WebView2.
pywebview>=5.0

# Self-hosted Hub API
fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
PyJWT>=2.8
bcrypt>=4.1
email-validator>=2.1
python-multipart>=0.0.9
httpx>=0.27

# Geospatial
rasterio>=1.3
pyproj>=3.6

# Photogrammetry
pycolmap>=0.6
open3d>=0.18
laspy[lazrs]>=2.5

# Flight / autopilot
pymavlink>=2.4.41

# Model inference (training extras live in training/requirements-train.txt)
onnxruntime>=1.17

# Object storage (S3 / MinIO backend)
boto3>=1.34
PyYAML>=6.0

# Report deliverables (PDF / DOCX)
reportlab>=4.0
python-docx>=1.1

# Test suite (tests/): python -m pytest
pytest>=8.0
celery>=5.3
redis>=5.0
