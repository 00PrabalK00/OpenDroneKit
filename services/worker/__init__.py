"""Processing workers: the Celery application and the pipeline task.

A package rather than a namespace directory, matching services/api. This file was
untracked for a while and the tests still passed, because it existed on the machine that
wrote them -- a fresh clone would have been the first place that mattered, and CI is a
fresh clone every time.
"""
